// Place your key bindings in this file to override the defaultsauto[]
[
    {
        "key": "shift+cmd+7",
        "command": "editor.action.commentLine",
        "when": "editorTextFocus && !editorReadonly"
    },
    {
        "key": "cmd+/",
        "command": "-editor.action.commentLine",
        "when": "editorTextFocus && !editorReadonly"
    },
    // GitHub Copilot
    {
        "key": "ctrl+f",
        "command": "github.copilot.interactiveEditor.fix"
    },
    {
        "key": "ctrl+e",
        "command": "github.copilot.interactiveEditor.explain"
    },
    {
        "key": "ctrl+d",
        "command": "github.copilot.interactiveEditor.generateDocs"
    },
    {
        "key": "ctrl+escape",
        "command": "github.copilot.toggleCopilot"
    }
    // Vim specific keybindings
    // When the parameterHints and the suggestWidget is visible the escape key no longer goes back to normal mode instead just closes the context-menu.
    // {
    //     "key": "escape",
    //     "command": "extension.vim_escape",
    //     "when": "editorTextFocus && vim.active && !inDebugRepl && !parameterHintsVisible && !suggestWidgetVisible"
    // },
    // {
    //     "key": "escape",
    //     "command": "-extension.vim_escape",
    //     "when": "editorTextFocus && vim.active && !inDebugRepl"
    // }
]